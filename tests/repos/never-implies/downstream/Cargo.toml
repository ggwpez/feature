[package]
name = "downstream"
version = "0.1.0"
edition = "2021"

[dependencies]
root = { path = "../root", default-features = false }

[features]
default = [
	"precondition"
]
precondition = [
	"root/forbidden",
]
